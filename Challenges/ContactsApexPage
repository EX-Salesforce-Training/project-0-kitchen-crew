<apex:page standardController="Contact" recordSetVar='contacts' >
    <apex:form>
    	<apex:pageBlock>
        	<apex:pageBlockTable value="{!contacts}" var="ctct">
                <apex:column value="{!ctct.name}" />
            </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>
