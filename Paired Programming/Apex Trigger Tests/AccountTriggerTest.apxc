// Write tests for both of the triggers you previously created in the last challenge. 
//  Ensure as close to 100% code coverage as possible and do single, bulk, and positive testing.
@isTest
public class AccountTriggerTest {
	@isTest
    public static void singleAccountTest(){
        // Test for account insertion, deletion, undelete and creating task
        Account testAcc = new Account(Name = 'Test');
        Database.SaveResult sr = Database.insert(testAcc, true);
        if(sr.isSuccess()){
            system.assert(true); 
        }
        Id testAccId = testAcc.Id;	// Id created after insert
        Database.DeleteResult dr = Database.delete(testAcc, true);
        if(dr.isSuccess()){
            system.assert(true);
        }
        system.debug('testAccID is: ' + testAccId);
        Database.UndeleteResult ur = Database.undelete(testAccId, true);
        if(ur.isSuccess()){
            system.assert(true); // the account was undeleted successfully
        }
        Task testTask = new Task(OwnerID = testAccId);
        if (testTask != null) {
            system.assert(true); // the task was created successfully
        }
    }
}
